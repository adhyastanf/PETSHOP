import { Card, CardContent } from '@/components/ui/card';

export default function CartSummary() {
  return (
    <Card className='self-start sticky top-20'>
      <CardContent>Cart Sumarry</CardContent>
    </Card>
  );
}
